#include "main.h"

/**
 * print_to_98 - main function
 * @n: n
 * Return: return 0
 *
 */

void print_to_98(int n)
{
		if (n < 98)
		{
			n++;
			_putchar(44);
			_putchar(32);
		}
		else if (n > 98)
		{
			n--;
			_putchar(44);
			_putchar(32);
		}
		else
		{
			_putchar(n);
		}
}
