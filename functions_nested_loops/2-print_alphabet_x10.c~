#include "main.h"
/**
 * print_alphabet_x10 -Prints alphabet 10 times
 *
 *Return: return 0
 */

void print_alphabet_x10(void)
{
	int a;
	int b = 0;

	while (b <= 9)
	{
		a = 'a';
		while (a <= 'z')
		{
			_putchar(a);
			b++;
		}
	 a++;
	_putchar('\n');
	return (0);
	}
}
