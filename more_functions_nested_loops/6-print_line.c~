#include "main.h"

/**
 * print_line - check the code
 * @n: n
 * Return: Always 0.
 */

void print_line(int n)
{
	char i[] = "_";

	if (n == 0)
	{
		_putchar ('\n');
	}
	else
	{
		_putchar (i[n]);
		_putchar ('\n');
	}
}
