#include "main.h"

/**
 * more_numbers - check the code
 *
 * Return: Always 0.
 */

void more_numbers(void)
{
	int i;
	int a = 0;

	while (a <= 10)
	{
		a++;
		_putchar (i);

		while ( i <= 14 )
		{
			i++;
			_putchar (i);
		}
	}
        _putchar ('\n');
}
